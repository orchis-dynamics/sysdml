# model

The system-dynamics model graph: `IR` and its parts (`IRStock`, `IRFlow`,
`IRAuxiliary`, `IRConnection`, `IRGraphicalFunction`, `IRTime`, `IRPosition`) plus
`CompileResult`, the compiler's output bundle of `IR` and diagnostics.
