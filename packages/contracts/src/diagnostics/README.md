# diagnostics

Semantic diagnostics raised after parsing: `DiagnosticCode` + `IRDiagnostic` from
compilation, and `SimDiagnosticCode` + `SimDiagnostic` from simulation. `IRDiagnostic`
carries an optional `Span` from the syntax domain.
